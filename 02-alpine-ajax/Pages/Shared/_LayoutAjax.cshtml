@* Lean layout for Alpine-AJAX requests: no <html>/<head>, no nav chrome —
   just the page body plus the live regions Alpine needs to find by id.
   The browser already has Tailwind + Alpine loaded from the first page. *@
@RenderBody()
<partial name="_LiveRegions" />
