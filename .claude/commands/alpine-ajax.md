This Claude command is an adapter for the shared repository docs.

Canonical guidance:

- `docs/alpine-ajax.md`

Use the Alpine-AJAX reference when implementing AJAX interactions, partial page
updates, side trays, inline validation, or any feature that uses `x-target`,
`$ajax()`, `x-sync`, `x-merge`, or Alpine-AJAX events.
